# Git
.git
.gitignore
.gitattributes

# Documentation
README.md
CLAUDE.md
LICENSE
FUNDING.yml
.github/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Dependencies (reinstalled inside the image)
node_modules/
electron/
server/node_modules/

# Root tooling not needed in the runtime image
package.json
package-lock.json
scripts/
src-tauri/

# Docker & deploy
Dockerfile
docker-compose.yml
docker-compose.build.yml
docker-compose.nas.yml
.dockerignore

# Secrets — must never be baked into the image
.env
*.p8
asc.p8

# Build artifacts
*.log
*.tmp

# UI-saved credentials (never bake into image)
server/data/
